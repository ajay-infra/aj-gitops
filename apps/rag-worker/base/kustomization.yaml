apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Resources mirror frontend/base — add component-specific manifests here:
#   deployment.yaml, service.yaml, httproute.yaml, pdb.yaml, serviceaccount.yaml
# Overlays patch: hostnames (all envs), replicas + pdb.minAvailable (prod)
resources: []
