# cloudability/metrics-agent — staging

agent:
  apiKeySecretRef:
    name: cloudability-api-key
    key:  api-key

  pollInterval: 300

  resources:
    requests:
      cpu:    50m
      memory: 64Mi
    limits:
      cpu:    200m
      memory: 128Mi
