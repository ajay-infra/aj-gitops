# hostNetwork is a pod-level escape, not a container securityContext one.
apiVersion: v1
kind: Pod
metadata: {name: bad-hostnet, namespace: frontend}
spec:
  hostNetwork: true
  containers:
    - name: app
      image: 555555555555.dkr.ecr.us-east-1.amazonaws.com/frontend:v1
