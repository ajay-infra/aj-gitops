apiVersion: v1
kind: Pod
metadata: {name: bad-limits, namespace: frontend}
spec:
  containers:
    - name: app
      image: REPLACE_WITH_ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/frontend:v1
      resources:
        requests: {cpu: 100m, memory: 128Mi}
